// Diferença entre = = e = = =
// = 3 vezes torna a condição mais restrita

let idade = '20'

if (idade == 20) {
  console.log('Ele tem vinte anos')
}

// No exemplo acima, o JS achou a condição sendo true mesmo que na variavel tenha sido declarodo o 20 dentro de uma string

// Caso tivesse sido usado = = =, não daria certo
