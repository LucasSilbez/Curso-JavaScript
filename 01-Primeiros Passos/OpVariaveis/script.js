let x = 0

x = x + 1 // estou definindo um novo valor a x usando operado matematico, resultado 1

console.log(x)

let y = 8

console.log(x + y) // somando o valor de x com o valor de y

// também posso criar uma nova variavel para operar

let soma = x + y

// Caso seja uma operação com mais numero, e eu queira que seja feito um determinado operado antes, uso o (), operadores dentro de () são preferencia

// Comportamento String:

let nome = 'Lucas'
let sobrenome = 'Bezerra'

let nomeCompleto = nome + ' ' + sobrenome // está sendo concatenado (string vazia é para ter espço entre os nomes)
