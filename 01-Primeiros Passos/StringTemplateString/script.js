// Template string eu posso declarar variaveis dentro de uma string
//É necessário usar ``
// para declarar, usar ${}

let nome = 'Lucas'
let sobrenome = 'Bezerra'

let nomeCompleto = `${nome} ${sobrenome}`

console.log('Meu nome é' + ' ' + nomeCompleto)
